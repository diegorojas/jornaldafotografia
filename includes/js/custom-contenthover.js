jQuery(document).ready(function(){ //on document.ready
	jQuery('.post').contenthover({
		data_selector: '.contenthover',	// The selector for the element that will be the content of the overlay element to show on hover 
		overlay_opacity:1,
	});
})

